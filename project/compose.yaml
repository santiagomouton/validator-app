services:
  server:
    container_name: validator-api
    image: docker.io/santlink/validator-api:${TAG}
    restart: unless-stopped

  proxy:
    container_name: nginx-proxy
    image: docker.io/santlink/validator-proxy:${TAG}
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    logging:
      driver: "json-file"

